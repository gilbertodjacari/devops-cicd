name: Primeiro Workflow

on:
  push:
    branches: [ main ]

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Mensagem de teste
        run: echo "Hello DevOps! Pipeline funcionando."